# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd

# Virtual envs
.venv/
venv/
env/
.qms-venv/
.python-version  # pyenv

# Build / packaging
build/
dist/
*.egg-info/
.eggs/

# --- Django ---
# Local settings & secrets
*.env
*.env.*        # .env.dev, .env.local, etc.
.envrc
.envrc.*
*.secret
*.secrets.json
secret*.json
settings_local.py
local_settings.py

# SQLite (keep if you really commit DB)
db.sqlite3
*.sqlite3
*.sqlite3-*
*.sqlite3-shm
*.sqlite3-wal

# Collected static & user uploads (keep your source assets in version control)
staticfiles/
static_root/
static_collected/
media/
!media/.gitkeep

# Logs & runtime
*.log
logs/
celerybeat-schedule
celerybeat.pid
celerybeat*.db
celery.pid
gunicorn*.pid
*.pid

# Cache / tooling
.cache/
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/
.nox/
.mypy_cache/
.dmypy.json
.pyre/
.ruff_cache/
.ipynb_checkpoints/

# Type checkers / formatters
pyrightconfig.json.tmp

# --- Node / Tailwind / Frontend ---
node_modules/
.pnpm-store/
.pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.browserslistrc.tmp
*.tsbuildinfo

# Build artifacts (keep your actual src under e.g. app/static/src)
# ضع مساراتك الفعلية هنا (مثال: theme/...)
theme/static/css/dist/
theme/static/js/dist/
assets/dist/
public/build/
build/
*.map
*.min.*.map

# إذا كنت تنتج CSS النهائي في مجلد ثابت معيّن، تجاهله
# مثال:
# project/static/dist/
# app_name/static/app_name/dist/

# --- Editors / OS ---
# VS Code
.vscode/
!.vscode/extensions.json
!.vscode/settings.sample.json

# JetBrains / PyCharm
.idea/
*.iml

# macOS / Windows
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Docker / Compose ---
# (If you template envs per environment, keep the samples tracked)
docker-compose.override.yml
.env.docker
.env.prod
.env.dev

# --- Certificates / keys (never commit real keys) ---
*.pem
*.key
*.crt
*.csr

# --- Misc ---
# lockfiles يجب أن تبقى مُتتبَّعة (لا تضفها هنا):
# package-lock.json / yarn.lock / pnpm-lock.yaml
